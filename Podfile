# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SpaceXInfo' do
  
  use_frameworks!

  pod 'ViperPoison', :git => 'git@gitlab.com:LisnicVictor/ViperPoison.git'
  pod 'LVResult', :git => 'git@gitlab.com:LisnicVictor/LVResult.git'
  pod 'LVNetworkAdapter', :git => 'git@gitlab.com:LisnicVictor/LVNetworkAdapter.git'

  target 'SpaceXInfoTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
